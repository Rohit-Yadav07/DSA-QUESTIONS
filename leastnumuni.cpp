#include"cp.cpp"
 int findLeastNumOfUniqueInts(vector<int>& arr, int k) {
        int n=arr.size();
        map<int,int> hash;
        for (int i = 0; i < n; i++)
        {
            hash[arr[i]]++;
        }
        
        
        
    }