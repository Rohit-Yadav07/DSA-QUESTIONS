#include<bits/stdc++.h>
using namespace std;
pair<int, int> getFloorAndCeil(vector<int> &a, int n, int x) {
	for (int i = 0; i < n; i++)
    {
        if (a[i]==x)
        {
           return {x,x};
        }
        
        
    }
    
		cout<<
	}
	
